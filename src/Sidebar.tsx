
const Sidebar = () => {
    const [sidebarWidth, setSidebarWidth] = useState(250); // Default width
    const isResizing = useRef(false); // Track if the user is dragging
    
    return ()
}

export default Sidebar